Imports MySql.Data.MySqlClient

Module comandosql

    Sub cargardata()
        ' Dim dataadapter As New MySqlDataAdapter("SELECT Nombre FROM candidato", cn)
        'Dim dataset As New DataSet

        'dataadapter.Fill(dataset, "candidato")

        'Votacion.ltbxcandidato.DataSource = dataset.Tables("candidato")
        'jnjjjjjjjjjjjjjjjjjjjjjjjjjjjjjjjjjjjj

        'Dim oComando As New MySqlCommand("SELECT Nombre,CodigoC FROM Elecciones where CodigoE ", cn)

        'cn.Open()

        'Data = oComando.ExecuteReader()
        ' Votacion.ltbxcandidato.Items.Clear()
        ' While Data.Read
        'Votacion.ltbxcandidato.Items.Add(Data("nombre") & " " & Data("CodigoC") & "")



        ' End While


    End Sub
   
    
End Module
